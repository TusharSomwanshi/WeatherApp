import React from 'react';
import logo from '../../images/weather_app.gif';

const Header = () => (
    <header className="App-header">
        <img src={logo} alt="logo" style={{height: '100px'}}/>
    </header>
);

export default Header;